package: multimodal
entry: tool_impl:MultimodalTool
category: vision
description: "Multi-modal synthesis and analysis (e.g., Fundus→OCT, Fundus→EyeGlobe) running in a subprocess."
shared:
  environment_ref: py312-multimodal
  runtime:
    load_mode: subprocess
    idle_timeout_s: 600
    device_policy: auto
    preload: false
  io:
    input_schema:
      image_path: path
      eye_category: string
      sampling_steps: integer
      SE: number
      AL: number
    output_schema:
      output_paths: object
      inference_time: number
  model_defaults:
    precision: auto
    lazy: true
variants:
  - variant: fundus2oct
    version: 0.1.0
    description: "Synthesize OCT from fundus (conditional generation)"
    params:
      task: fundus2oct
  - variant: fundus2eyeglobe
    version: 0.1.0
    description: "Synthesize EyeGlobe from fundus (conditional generation)"
    params:
      task: fundus2eyeglobe
metadata:
  authors: ["EyeAgent Team"]
  license: Apache-2.0
notes:
  - 0.1.0 multimodal initial (fundus2oct, fundus2eyeglobe)
