def classify_image(image_base64: str, model_path: str = "default_classification_model.pth") -> str:
    """Classify an image (dummy result)."""
    # Pretend we did some ML inference
    return "dog"
